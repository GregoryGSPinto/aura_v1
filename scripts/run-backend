#!/bin/zsh
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$REPO_ROOT/aura/backend"
if [ -f .env ]; then
  export $(grep -v '^#' .env | xargs)
fi
python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
