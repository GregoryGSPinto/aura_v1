#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export AURA_BACKEND_HOST="${AURA_BACKEND_HOST:-127.0.0.1}"
export AURA_BACKEND_RELOAD="${AURA_BACKEND_RELOAD:-0}"

exec "$SCRIPT_DIR/run-backend"
